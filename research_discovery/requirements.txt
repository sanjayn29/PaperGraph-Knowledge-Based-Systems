# ──────────────────────────────────────────────────────────────
#  PaperGraph — Python dependencies
#  Install with: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# Core
streamlit>=1.35.0
python-dotenv>=1.0.0

# PDF Processing
PyMuPDF>=1.24.0

# Data & Numerics
numpy>=1.26.0
pandas>=2.2.0

# Embeddings
sentence-transformers>=3.0.0

# Graph
networkx>=3.3

# Visualization
plotly>=5.22.0

# LLM — Gemini (new google-genai SDK, replaces deprecated google-generativeai)
google-genai>=1.0.0

# Utilities
python-dateutil>=2.9.0

# Evaluation metrics (AUC, AP, P@K, NDCG@K)
scikit-learn>=1.4.0

# Testing
pytest>=8.2.0

# ──────────────────────────────────────────────────────────────
# Optional — SE-TGN + GCN components
# ──────────────────────────────────────────────────────────────
# SE-TGN requires PyTorch (CPU or GPU). The app works without it
# (falls back to graph + semantic scoring). Install to enable the
# temporal graph network and GCN baseline.
#
# Windows CPU-only install:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-geometric
#
# Uncomment to include in requirements:
# torch>=2.3.0
# torch-geometric>=2.5.0
